[pytest]
addopts = --strict-config --strict-markers -ra
xfail_strict = true
filterwarnings =
    error
markers =
    integration: end-to-end contract, routing, state, validation, and execution integration
    durability: atomicity and recovery durability subset
