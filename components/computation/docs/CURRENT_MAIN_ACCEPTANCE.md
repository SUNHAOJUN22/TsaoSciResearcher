# TsaoSciComputation current-main acceptance / 当前主线验收

This annex is generated by `scripts/refresh-current-main-readme.mjs`.
本附录由仓库脚本生成，记录当前 `main` 的代码—数学—证据关系。

## Formula-to-code anchors / 公式到代码锚点

| Implementation path / 实现路径 | Contract |
|---|---|
| `tsao_computation/adapters/base.py` | current implementation anchor |
| `tsao_computation/execution/runner.py` | current implementation anchor |
| `tsao_computation/validation/numerical.py` | current implementation anchor |

## Acceptance identity / 验收身份

$$
H_accept = SHA256(code ∥ docs ∥ visuals ∥ tests ∥ environment)
$$

PASS is valid only for the immutable SHA tested by the workflow. Any later commit requires a new run.
PASS 只对工作流实际测试的不可变 SHA 有效；任何后续提交都必须重新运行。

## Boundary / 边界

- The repository is a computation control plane and qualification framework; third-party DFT, MD, CFD, FEM and process solvers remain EXTERNAL_HOLD.
- 仓库是计算控制面与资格框架；第三方 DFT、MD、CFD、FEM 和流程求解器保持 EXTERNAL_HOLD。
