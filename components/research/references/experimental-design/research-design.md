# Research design

Map each hypothesis to a discriminating observation, experiment, computation or dataset. Define success and failure criteria before execution. Use triangulation when multiple evidence modes can fail differently. Include negative controls, positive controls and baseline methods where relevant.

A technical route should show dependencies, decision gates, fallback methods, required artifacts and validation at every stage.
