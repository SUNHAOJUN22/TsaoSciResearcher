[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "aspenops-nexus"
version = "2.0.0"
description = "A deterministic, process-isolated and license-aware Aspen execution fabric for AI coding agents."
readme = "README.md"
requires-python = ">=3.11"
license = {file = "LICENSE"}
authors = [{name = "SUNHAOJUN22"}]
keywords = ["aspen-plus", "hysys", "mcp", "codex", "claude-code", "process-simulation"]
classifiers = [
  "Development Status :: 4 - Beta",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Operating System :: Microsoft :: Windows",
  "Operating System :: POSIX :: Linux",
  "Topic :: Scientific/Engineering",
]
dependencies = ["psutil>=5.9"]

[project.optional-dependencies]
windows = ["pywin32>=306; platform_system == 'Windows'"]
agent = ["mcp>=1.9,<2"]
signing = ["cryptography>=50"]
dev = [
  "build>=1.2",
  "cryptography>=50",
  "mypy>=1.15",
  "pytest>=8.3",
  "pytest-cov>=6.0",
  "ruff>=0.11",
  "types-psutil>=5.9",
  "types-pywin32>=306",
]

[project.scripts]
aspenops = "aspenops_nexus.cli_bootstrap:main"
aspenops-mcp = "aspenops_nexus.mcp_server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/aspenops_nexus"]

[tool.hatch.build]
include = [
  "src/aspenops_nexus/**/*.py",
  "src/aspenops_nexus/py.typed",
  "src/aspenops_nexus/data/*.json",
]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]

[tool.ruff.lint.per-file-ignores]
"tests/test_delivery_bundle.py" = ["F841"]  # second build is validated byte-for-byte

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["aspenops_nexus"]

[tool.pytest.ini_options]
minversion = "8.3"
addopts = "-q --strict-markers --strict-config"
testpaths = ["tests"]
xfail_strict = true
filterwarnings = ["error::ResourceWarning"]
markers = ["aspen_real: requires licensed Windows Aspen installation"]
