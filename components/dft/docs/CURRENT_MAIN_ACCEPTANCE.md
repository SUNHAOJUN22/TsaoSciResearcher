# TsaoDFT Skill current-main acceptance / 当前主线验收

Generated by `scripts/refresh-current-main-readme.mjs`. 本附录记录当前 `main` 的代码—数学—证据关系。

| Implementation path / 实现路径 | Contract |
|---|---|
| `skills/tsao-structure-prep/scripts/neighbor_list.py` | current implementation anchor |
| `scripts/build_release_acceptance.py` | current implementation anchor |
| `scripts/capture_compute_contract_evidence.py` | current implementation anchor |

$$
H_accept = SHA256(code ∥ docs ∥ visuals ∥ tests ∥ environment)
$$

PASS is valid only for the immutable tested SHA. PASS 只对实际测试的不可变 SHA 有效。

- Software gates validate the control plane, reference numerics and evidence contracts only; external engines such as Gaussian, VASP, QE and CP2K are not executed here and remain EXTERNAL_HOLD.
- 软件门禁只验证控制面、参考数值与证据合同；Gaussian、VASP、QE、CP2K 等外部引擎未在此自动执行，保持 EXTERNAL_HOLD。
