# Definition of done

- Four public engine entry points share one fatal-precedence contract.
- Multi-job and truncated output states are explicit and fail closed.
- TST molecularity and standard-state units follow the documented equation.
- Parser quantities cannot confuse summary scalars with full vectors or tensors.
- Dataset, model, code, environment, applicability domain, uncertainty, holdout, and independent approval are hash-bound.
- Trainer output cannot self-issue `validated` or `accepted`.
- Missing external engine evidence remains `EXTERNAL_DFT_EXECUTION_NOT_VERIFIED`.
