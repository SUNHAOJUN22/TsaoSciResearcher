# Structure Formats

Prefer SDF/MOL2 for molecular bond orders, XYZ for coordinates only, CIF/POSCAR for periodic cells, and PDB only when its naming/topology semantics are appropriate. Always record length units and periodic cell vectors.
