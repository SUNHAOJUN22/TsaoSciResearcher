# Provenance and Reproducibility

Record hashes of inputs/outputs, code commit, software version, environment variables, scheduler metadata, job ID, resource use, attempts, checkpoints and validation result.
