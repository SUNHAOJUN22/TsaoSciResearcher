# Failure and Recovery

Match exact error signatures, diagnose the earliest causal failure, change one setting, and preserve the prior attempt. SCF, geometry, memory, disk, timeout, MPI and missing-file failures require different responses.
