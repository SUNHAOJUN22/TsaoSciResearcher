# Features and Models

Start with transparent baselines: physical descriptors or fingerprints plus linear/tree models. Use graph/3D models when sample size and invariances justify them. Match feature generation to the same validated structure/state used for the target.
