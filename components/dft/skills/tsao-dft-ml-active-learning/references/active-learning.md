# Active Learning

Define candidate pool, acquisition function, uncertainty source, diversity metric, constraints, batch size, DFT budget, validation gate and stop criterion. Selected candidates return to the full calculation/acceptance workflow.
