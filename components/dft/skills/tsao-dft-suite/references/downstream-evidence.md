# Downstream Evidence

ML datasets and kinetic networks consume only accepted DFT artifacts with method/fidelity provenance. A model prediction, acquisition score, fitted kinetic parameter, or reactor result cannot retroactively validate the DFT calculation that generated its inputs.
