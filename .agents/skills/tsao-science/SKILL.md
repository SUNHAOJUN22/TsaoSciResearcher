---
name: tsao-science
description: Unified scientific research, computation, DFT, polymer process modeling and materials workspace. Route the research question, use explicit quantities and fixed local adapters, preserve evidence and keep external execution separately authorized.
---
# TsaoScience

Use the root README and `python -m tsao_science doctor` to inspect the actual checkout.
Use `route` for research and computation routing, `plan` before execution, and `run --execute-local`
only when the user has authorized that bounded local computation. Use `component` for a declared
advanced domain command, preserving its original authorization gates.

Load only the relevant component instructions. Reasoning methods are subordinate modules, not
independent products; TRIZ remains explicit-only. Never load evaluation holdouts as runtime context.
Keep observed, simulated, reference and hypothetical results distinct. Do not invent engine runs,
licenses, batch identities, measurements, citations, independent review or public-data permissions.
