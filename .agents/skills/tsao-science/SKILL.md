---
name: tsao-science
description: Unified scientific research, computation, DFT, polymer process modeling and materials workspace. Route the research question, use explicit quantities and fixed local adapters, preserve evidence and keep external execution separately authorized.
---
# TsaoScience

Use the root README and `python -m tsao_science doctor` to inspect the actual checkout.
Use `route` for research and computation routing, `plan` before execution, and `run --execute-local`
only when the user has authorized that bounded local computation. Use `component` for a declared
advanced domain command, preserving its original authorization gates.

Load only the relevant component instructions. Reasoning methods are subordinate modules, not
independent products; TRIZ remains explicit-only. Never load evaluation holdouts as runtime context.
Keep observed, simulated, reference and hypothetical results distinct. Do not invent engine runs,
licenses, batch identities, measurements, citations, independent review or public-data permissions.


## Shared protocol in astra-pro-2

Use `catalog --query ...` to find preserved source methods. Catalog descriptions are not executable permissions: select execution only from `capabilities`. Components, examples and payload schemas are single-source registries under contracts/. Resolve task references and validate inputs/outputs through the root engine instead of reproducing scientific formulas in the Skill.

Use `verify RESULT --ledger LEDGER` to check run/2 task and event binding. A self-consistent record, observed-source label or hash chain is not authentication of an experiment. Never relabel a reference-model result as measured. The workbench import displays validated records without silently inserting experimental measurements.

Use the extended software qualification profile for changes. Do not delete inherited tests, reduce their thresholds, replace controlled classification, or publish full-source bundles to make a pipeline pass. Six redirect repositories are historical objects, not active development targets.
