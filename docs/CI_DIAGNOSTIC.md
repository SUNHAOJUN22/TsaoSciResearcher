# CI diagnostic trigger

This file exists only to trigger the unchanged GitHub Actions workflow on a pull request so that platform-specific logs can be inspected. It does not change runtime behavior.
