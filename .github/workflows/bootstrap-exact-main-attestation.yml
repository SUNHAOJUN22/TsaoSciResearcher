name: Bootstrap exact main attestation

on:
  issues:
    types: [opened]

permissions:
  contents: write
  issues: write

concurrency:
  group: bootstrap-exact-main-attestation
  cancel-in-progress: false

env:
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONNOUSERSITE: "1"
  PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"

jobs:
  bootstrap:
    if: >-
      github.actor == 'SUNHAOJUN22' &&
      github.event.issue.title == '[TSR] bootstrap exact main attestation'
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Mark bootstrap running
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          printf 'status=running\nworkflow-run=%s\n' "$GITHUB_RUN_ID" > /tmp/issue-body.txt
          gh issue edit "$ISSUE_NUMBER" --repo "$GITHUB_REPOSITORY" --body-file /tmp/issue-body.txt

      - name: Checkout current main
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: main
          fetch-depth: 0

      - name: Build final documented candidate
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)"
          test -e .github/workflows/main-attestation.yml
          python - <<'PATCH_PY'
          from pathlib import Path

          for readme_name in ('README.md', 'README_EN.md'):
              path = Path(readme_name)
              text = path.read_text(encoding='utf-8')
              badge = '[![Exact main attestation](https://github.com/SUNHAOJUN22/TsaoSciResearcher/actions/workflows/main-attestation.yml/badge.svg?branch=main)](https://github.com/SUNHAOJUN22/TsaoSciResearcher/actions/workflows/main-attestation.yml)'
              ci_badge = '[![CI](https://github.com/SUNHAOJUN22/TsaoSciResearcher/actions/workflows/ci.yml/badge.svg?branch=main)](https://github.com/SUNHAOJUN22/TsaoSciResearcher/actions/workflows/ci.yml)'
              if badge not in text:
                  text = text.replace(ci_badge, ci_badge + ' ' + badge, 1)
              marker = 'Checked-in `docs/VALIDATION_EVIDENCE.json` remains deliberately `preflight/PARTIAL`; commit-bound PASS evidence is produced externally by CI to avoid self-referential commit claims.'
              addition = marker + '\n\nThe content-read-only **Exact main attestation** workflow independently validates the immutable current `main` SHA and uploads commit-bound evidence without modifying that commit.'
              if addition not in text:
                  text = text.replace(marker, addition, 1)
              path.write_text(text, encoding='utf-8', newline='\n')

          path = Path('README.zh-CN.md')
          text = path.read_text(encoding='utf-8')
          badge = '[![最终 main 精确证明](https://github.com/SUNHAOJUN22/TsaoSciResearcher/actions/workflows/main-attestation.yml/badge.svg?branch=main)](https://github.com/SUNHAOJUN22/TsaoSciResearcher/actions/workflows/main-attestation.yml)'
          ci_badge = '[![CI](https://github.com/SUNHAOJUN22/TsaoSciResearcher/actions/workflows/ci.yml/badge.svg?branch=main)](https://github.com/SUNHAOJUN22/TsaoSciResearcher/actions/workflows/ci.yml)'
          if badge not in text:
              text = text.replace(ci_badge, ci_badge + ' ' + badge, 1)
          marker = '仓库内 `docs/VALIDATION_EVIDENCE.json` 有意保留为 `preflight/PARTIAL`；与提交绑定的 PASS 证明由 CI 外部生成，避免在提交内部制造自引用 SHA。'
          addition = marker + '\n\n内容只读的 **Exact main attestation** 工作流会独立验证不可再修改的当前 `main` SHA，并上传与该提交绑定的外部证据，而不会反过来改写被验证的提交。'
          if addition not in text:
              text = text.replace(marker, addition, 1)
          path.write_text(text, encoding='utf-8', newline='\n')

          facts = Path('scripts/build_readme_facts.py')
          text = facts.read_text(encoding='utf-8')
          anchor = 'FACTS_PATH = ROOT / "docs/README_FACTS.json"\n'
          if 'MAIN_ATTESTATION_WORKFLOW' not in text:
              text = text.replace(anchor, anchor + 'MAIN_ATTESTATION_WORKFLOW = ROOT / ".github/workflows/main-attestation.yml"\n', 1)
          marker = '    required_docs = [\n'
          checks = '''    if not MAIN_ATTESTATION_WORKFLOW.is_file() or MAIN_ATTESTATION_WORKFLOW.is_symlink():
        errors.append("exact main attestation workflow is missing or unsafe")
    badge_token = "actions/workflows/main-attestation.yml/badge.svg?branch=main"
    if badge_token not in english:
        errors.append("README.md is missing the exact main attestation badge")
    if badge_token not in chinese:
        errors.append("README.zh-CN.md is missing the exact main attestation badge")

'''
          if 'exact main attestation workflow is missing or unsafe' not in text:
              text = text.replace(marker, checks + marker, 1)
          facts.write_text(text, encoding='utf-8', newline='\n')

          tests = Path('tests/test_supply_chain.py')
          text = tests.read_text(encoding='utf-8')
          test_name = 'test_exact_main_attestation_workflow_is_read_only_and_commit_bound'
          if test_name not in text:
              text += '''


def test_exact_main_attestation_workflow_is_read_only_and_commit_bound() -> None:
    path = ROOT / ".github/workflows/main-attestation.yml"
    workflow = path.read_text(encoding="utf-8")
    assert "contents: read" in workflow
    assert "contents: write" not in workflow
    assert "[TSR] attest current main" in workflow
    assert "test \"$(git rev-parse HEAD)\" = \"$GITHUB_SHA\"" in workflow
    assert "build_publication_attestation.py --commit \"$GITHUB_SHA\"" in workflow
    assert "exact-main-attestation-${{ github.sha }}" in workflow
    assert "git push" not in workflow
    assert "actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd" in workflow
    assert "actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405" in workflow
    assert "actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a" in workflow
'''
          tests.write_text(text, encoding='utf-8', newline='\n')

          Path('.github/workflows/bootstrap-exact-main-attestation.yml').unlink()
          PATCH_PY
          test ! -e .github/workflows/bootstrap-exact-main-attestation.yml
          test -e .github/workflows/main-attestation.yml

      - name: Set up locked Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-ci.lock

      - name: Install exact dependencies
        run: |
          python -m pip install --disable-pip-version-check -r requirements-ci.lock
          python -m pip install --disable-pip-version-check -e . --no-deps
          python -m pip check

      - name: Normalize modified Python
        run: |
          python -m ruff check --fix scripts/build_readme_facts.py tests/test_supply_chain.py
          python -m ruff format scripts/build_readme_facts.py tests/test_supply_chain.py
          python -m ruff format --check scripts tsao_researcher tests
          python -m ruff check scripts tsao_researcher tests

      - name: Refresh candidate generated artifacts
        run: |
          python scripts/build_readme_facts.py --write
          python scripts/build_sbom.py --write
          python scripts/build_validation_evidence.py --write
          python scripts/build_test_dashboard.py --write
          python scripts/build_research_quality_dashboard.py --write
          python scripts/build_engineering_report.py --write
          python scripts/generate_checksums.py --write

      - name: Candidate repository and documentation audit
        run: |
          mkdir -p artifacts
          python -m compileall -q scripts tsao_researcher tests
          python scripts/sync_version.py --check
          python scripts/validate_schemas.py
          python scripts/audit_repository.py
          python scripts/validate_structure.py
          python scripts/build_readme_facts.py --check
          python scripts/build_sbom.py --check
          python scripts/build_validation_evidence.py --check
          python scripts/build_test_dashboard.py --check
          python scripts/build_research_quality_dashboard.py --check
          python scripts/build_engineering_report.py --check
          python scripts/generate_checksums.py --check
          python scripts/build_capability_index.py --check
          python scripts/route_task.py --self-test
          python scripts/validate_figure.py examples/figure-contract.json
          python scripts/validate_computation_strategy.py examples/first-principles-strategy.json
          mkdocs build --strict

      - name: Complete regression and coverage
        run: |
          python -m pytest -q -p hypothesis.extra.pytestplugin --junitxml=artifacts/junit.xml
          python -m pytest -q -p hypothesis.extra.pytestplugin -p pytest_cov --ignore=tests/test_import_isolation.py --cov=tsao_researcher --cov-branch --cov-report=term-missing --cov-report=json:artifacts/coverage.json --cov-report=xml:artifacts/coverage.xml

      - name: Order, static, security and dependency gates
        env:
          TSR_TEST_ORDER_SEED: "20260730"
        run: |
          python -m pytest -q -p hypothesis.extra.pytestplugin -p tests.reverse_order_plugin
          python -m pytest -q -p hypothesis.extra.pytestplugin -p tests.random_order_plugin
          python -m ruff format --check scripts tsao_researcher tests
          python -m ruff check scripts tsao_researcher tests
          python -m mypy scripts tsao_researcher
          python -m bandit -q -lll -r scripts tsao_researcher
          python -m pip list --format=freeze --exclude-editable | LC_ALL=C sort > artifacts/resolved-environment.lock
          python -m pip_audit --strict -r artifacts/resolved-environment.lock --format cyclonedx-json --output artifacts/resolved-environment-sbom.json
          python scripts/build_sbom.py --check

      - name: Mutation, performance and live metrics
        id: metrics
        env:
          RUN_ID: ${{ github.run_id }}
        run: |
          python scripts/run_mutation_smoke.py --json-out artifacts/mutation-results.json
          python scripts/performance_smoke.py --json-out artifacts/performance.json
          python scripts/check_quality_baseline.py
          python - <<'METRICS_PY' >> "$GITHUB_OUTPUT"
          import json
          import os
          import re
          from pathlib import Path

          quality = json.loads(Path('artifacts/quality-current.json').read_text(encoding='utf-8'))
          tests = quality['tests']
          coverage = quality['coverage']
          mutation = quality['mutation']
          run_id = os.environ['RUN_ID']

          english = Path('README.md').read_text(encoding='utf-8')
          english = re.sub(r"The published 0\.7\.0 tree was verified by GitHub Actions run `\d+`", f"The published 0.7.0 tree was verified by GitHub Actions run `{run_id}`", english, count=1)
          english = re.sub(r"\| Tests \| \*\*\d+ passed; \d+ failed; \d+ errors; \d+ skipped\*\* \|", f"| Tests | **{tests['tests']} passed; {tests['failures']} failed; {tests['errors']} errors; {tests['skipped']} skipped** |", english, count=1)
          english = re.sub(r"\| Project line coverage \| \*\*[0-9.]+%\*\* \|", f"| Project line coverage | **{coverage['line_percent']:.3f}%** |", english, count=1)
          english = re.sub(r"\| Branch coverage \| \*\*[0-9.]+%\*\* \|", f"| Branch coverage | **{coverage['branch_percent']:.3f}%** |", english, count=1)
          english = re.sub(r"\| Critical mutation tests \| \*\*\d+ / \d+ killed; \d+ survivors\*\* \|", f"| Critical mutation tests | **{mutation['killed']} / {mutation['total']} killed; {mutation['survivors']} survivors** |", english, count=1)
          Path('README.md').write_text(english, encoding='utf-8', newline='\n')
          Path('README_EN.md').write_text(english, encoding='utf-8', newline='\n')

          chinese = Path('README.zh-CN.md').read_text(encoding='utf-8')
          chinese = re.sub(r"已发布的 0\.7\.0 主分支代码已由 GitHub Actions 运行 `\d+`", f"已发布的 0.7.0 主分支代码已由 GitHub Actions 运行 `{run_id}`", chinese, count=1)
          chinese = re.sub(r"\| 测试 \| \*\*\d+ 通过；\d+ failure；\d+ error；\d+ skip\*\* \|", f"| 测试 | **{tests['tests']} 通过；{tests['failures']} failure；{tests['errors']} error；{tests['skipped']} skip** |", chinese, count=1)
          chinese = re.sub(r"\| 项目综合覆盖率 \| \*\*[0-9.]+%\*\* \|", f"| 项目综合覆盖率 | **{coverage['line_percent']:.3f}%** |", chinese, count=1)
          chinese = re.sub(r"\| 分支覆盖率 \| \*\*[0-9.]+%\*\* \|", f"| 分支覆盖率 | **{coverage['branch_percent']:.3f}%** |", chinese, count=1)
          chinese = re.sub(r"\| 关键 Mutation \| \*\*\d+ / \d+ killed；\d+ survivor\*\* \|", f"| 关键 Mutation | **{mutation['killed']} / {mutation['total']} killed；{mutation['survivors']} survivor** |", chinese, count=1)
          Path('README.zh-CN.md').write_text(chinese, encoding='utf-8', newline='\n')

          print(f"tests={tests['tests']}")
          print(f"line={coverage['line_percent']:.3f}")
          print(f"branch={coverage['branch_percent']:.3f}")
          print(f"mutation={mutation['killed']}/{mutation['total']}")
          print(f"survivors={mutation['survivors']}")
          METRICS_PY

      - name: Refresh and reconfirm final candidate
        run: |
          python scripts/build_readme_facts.py --write
          python scripts/build_sbom.py --write
          python scripts/build_validation_evidence.py --write
          python scripts/build_test_dashboard.py --write
          python scripts/build_research_quality_dashboard.py --write
          python scripts/build_engineering_report.py --write
          python scripts/generate_checksums.py --write
          python scripts/build_readme_facts.py --check
          python scripts/build_validation_evidence.py --check
          python scripts/generate_checksums.py --check
          mkdocs build --strict
          python -m pytest -q -p hypothesis.extra.pytestplugin

      - name: Deterministic release and distributions
        run: |
          python scripts/package_release.py --out dist-a
          python scripts/package_release.py --out dist-b
          cmp "dist-a/TsaoSciResearcher-v$(cat VERSION).zip" "dist-b/TsaoSciResearcher-v$(cat VERSION).zip"
          python scripts/validate_release.py
          python -m build --sdist --wheel --outdir dist-python
          python scripts/validate_distribution.py dist-python

      - name: Commit verified bootstrap to main
        id: publish
        run: |
          set -euo pipefail
          rm -rf .coverage .pytest_cache .hypothesis artifacts dist-a dist-b dist-python site build
          find . -type d -name __pycache__ -prune -exec rm -rf {} +
          test ! -e .github/workflows/bootstrap-exact-main-attestation.yml
          test -e .github/workflows/main-attestation.yml
          git add -A
          git -c core.whitespace=-blank-at-eof diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'ci: add exact current-main attestation'
          commit="$(git rev-parse HEAD)"
          git push origin HEAD:main
          echo "commit=${commit}" >> "$GITHUB_OUTPUT"

      - name: Report bootstrap result
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          COMMIT: ${{ steps.publish.outputs.commit }}
          TESTS: ${{ steps.metrics.outputs.tests }}
          LINE: ${{ steps.metrics.outputs.line }}
          BRANCH: ${{ steps.metrics.outputs.branch }}
          MUTATION: ${{ steps.metrics.outputs.mutation }}
          SURVIVORS: ${{ steps.metrics.outputs.survivors }}
        run: |
          cat > /tmp/issue-body.txt <<EOF
          status=bootstrap-published
          commit=${COMMIT}
          workflow-run=${GITHUB_RUN_ID}
          tests=${TESTS}
          line-coverage=${LINE}
          branch-coverage=${BRANCH}
          mutation=${MUTATION}
          survivors=${SURVIVORS}
          next=trigger-[TSR]-attest-current-main
          all-gates=PASS
          EOF
          sed -i 's/^          //' /tmp/issue-body.txt
          gh issue edit "$ISSUE_NUMBER" --repo "$GITHUB_REPOSITORY" --body-file /tmp/issue-body.txt

      - name: Report failure
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          printf 'status=bootstrap-failed\nworkflow-run=%s\n' "$GITHUB_RUN_ID" > /tmp/issue-body.txt
          gh issue edit "$ISSUE_NUMBER" --repo "$GITHUB_REPOSITORY" --body-file /tmp/issue-body.txt
